# Lost & Found Matcher

Image-based lost and found matching using CLIP embeddings and vector similarity search.

## Stack
- FastAPI backend
- Postgres + pgvector
- CLIP (transformers) for image/text embeddings
- React + TypeScript frontend

## Status
In development.